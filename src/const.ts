export enum AppRoute {
  Login = 'login',
  Main = '/',
  Favorites = 'favorites',
  Offer = 'offer/:id',
}

export const authorizationStatus = {
  Auth: true,
  NoAuth: false,
};

export const CountStar: number = 5;
